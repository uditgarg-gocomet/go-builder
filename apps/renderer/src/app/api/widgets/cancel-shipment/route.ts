// Next.js requires this file to exist at this path. The actual handler —
// gocomet fetch, header attach, response normalisation — lives in
// `@portal/widgets/api`. Edits to the API logic happen there, not here.

export { POST } from '@portal/widgets/api'
